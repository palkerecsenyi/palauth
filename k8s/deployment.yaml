---
apiVersion: v1
kind: Secret
metadata:
  name: palauth-session-secret
  namespace: default
  annotations:
    secret-generator.v1.mittwald.de/autogenerate: value

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: palauth
  namespace: default
  labels:
    app: palauth
spec:
  selector:
    matchLabels:
      app: palauth
  template:
    metadata:
      labels:
        app: palauth
    spec:
      imagePullSecrets:
        - name: paltiverse-docker
      containers:
        - name: palauth
          image: gitlab.palk.me:5050/paltiverse/palauth:$CI_COMMIT_SHA
          ports:
            - containerPort: 8080
          resources:
            limits:
              cpu: 500m
            requests:
              cpu: 200m
          env:
            - name: PORT
              value: "8080"
            - name: PAL_HOSTNAME
              value: auth.palk.me
            - name: PAL_OIDC_ID
              value: https://auth.palk.me
            - name: PAL_CAPTCHA_URL
              value: https://captcha.palk.me
            - name: PAL_CAPTCHA_KEY
              valueFrom:
                secretKeyRef:
                  name: friendly-captcha-api-key
                  key: api-key
            - name: PAL_SECRET
              valueFrom:
                secretKeyRef:
                  name: palauth-session-secret
                  key: value
            - name: PAL_PUBLIC_JWK
              valueFrom:
                secretKeyRef:
                  name: palauth-jwk
                  key: public
            - name: PAL_PRIVATE_JWK
              valueFrom:
                secretKeyRef:
                  name: palauth-jwk
                  key: private
            - name: PAL_MAILGUN_KEY
              valueFrom:
                secretKeyRef:
                  name: mailgun-api-key
                  key: api-key
            - name: PAL_MAILGUN_HOST
              value: https://auth.palk.me
            - name: PAL_REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: palauth-redis-url
                  key: redis-url
            - name: PAL_DB_STRING
              valueFrom:
                secretKeyRef:
                  name: palauth-db-url
                  key: url
            - name: NODE_ENV
              value: production
---
apiVersion: v1
kind: Service
metadata:
  name: palauth-svc
  namespace: default
spec:
  type: NodePort
  ports:
    - targetPort: 8080
      port: 8081
  selector:
    app: palauth

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: palauth-hpa
  namespace: default
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: palauth
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 50
