mixin two_fa_label(factor)
    if typeof factor === "string"
        if factor === "SecurityKey"
            | Security key
        else if factor === "TOTP"
            | Authenticator app
        else
            | Unknown
    else
        - const type = factor.type
        if type === "SecurityKey"
            | Security key (#{factor.keyNickname}
            if factor.isPasskey
                | &nbsp;&bull;&nbsp;
                strong passkey
            | )
        else if type === "TOTP"
            | Authenticator app
        else
            | Unknown
